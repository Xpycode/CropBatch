# Build Workflow

When building a modified version of the app:

1. **Kill the current running app** - `stop_mac_app`
2. **Clean the build folder** - `clean`
3. **Build and run** - `build_run_macos`

This ensures the latest changes are properly compiled and avoids stale build artifacts.