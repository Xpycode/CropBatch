# CropBatch v1.4 Test Checklist
## Grid Split (new feature)
- [x] Enable grid (e.g. 3x3), export single image — 9 tiles created
- [x] Tile filenames use naming suffix pattern (image_1_1.png, image_1_2.png, etc.)
- [x] Grid overlay visible in editor when enabled
- [x] Disable grid, export — single file output, no tiles
- [x] Grid + rename pattern combo — tiles named correctly
- [x] Grid + overwrite original mode — shows error message
### Grid Split CLI
- [ ] CropBatch --grid-rows 3 --grid-cols 3 input.png -o output/ — 9 tiles
- [ ] CLI without grid flags — normal single-file export
## Watermark Error Feedback (new)
- [x] Drag non-image file (.txt, .pdf) onto watermark drop zone — red error caption appears
- [x] Then drag valid PNG — error clears, watermark loads
- [x] Use file picker to load watermark — works as before
## Image Switching (bug fix)
- [x] Load 3+ images, click through thumbnails rapidly — no flash or flicker
- [x] Switch images at Fit zoom mode
- [x] Switch images at Fit Width zoom mode
- [x] Switch images at Actual Size zoom mode
## Core Crop Workflow
- [x] Load images, adjust crop rect, export — output matches expected crop
- [ ] Crop with corner radius — output is PNG with rounded corners
- [x] Undo/redo crop changes
- [x] Aspect ratio guide overlay displays correctly
## Blur Tool
- [ ] Switch to blur tool, add blur region, switch back to crop — blur preview visible
- [ ] Export with blur regions — blurred areas in output
## Transform
- [x] Rotate 90, 180, 270
- [x] Flip horizontal, flip vertical
- [x] Transform + crop + export — correct output
## Export Settings
- [x] JPEG export with quality slider
- [x] PNG export
- [x] Resize enabled — output dimensions correct
- [x] Rename pattern — filenames match pattern
- [x] Export to same folder vs custom folder
- [x] Batch export multiple images
## Watermark
- [x] Load watermark image, export — watermark appears in output
- [x] Watermark position options (corners, center)
- [x] Watermark with text variables
## Folder Watcher
- [ ] Enable folder watcher, drop file into watched folder — processes after ~0.5s
## Sparkle Update
- [ ] App launches without Sparkle errors in console
- [ ] Check for Updates menu item works