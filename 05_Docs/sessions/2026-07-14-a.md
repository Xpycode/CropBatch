# Session: 2026-07-14

## Goal
Unblock CropBatch v1.6 auto-update (M4 Pro session). PROJECT_STATE said: re-sign the v1.6 DMG
with the correct Sparkle key on this Mac, swap the appcast signature, push.

## Progress
- **Disproved the documented unblock.** The v1.6 appcast was signed with the wrong Sparkle key,
  but the "re-sign on the M4 Pro" premise is invalid — the correct key isn't on this Mac either.
  - App trusts `SUPublicEDKey = o388Mk7…` (audit "Group A", shared by CropBatch/ScreenshotFromVideos/
    QuickMotion/AspectRatioUnifier). Keychain account `ed25519` (its designed home) is **empty**;
    default readable Sparkle slot holds account `penumbra`.
  - M1 Max's signable slot holds `eH6jo…` ("Group B", syncthingStatus/DiskVerdict) — that's what
    mis-signed v1.6.
  - Backup file gone; Time Machine not configured; Syncthing `.stversions` + git history have no key.
  - **User confirmed no Strongbox/other backup → Group A key is LOST.** (Root reference:
    `~/ProgrammingProjects/1-macOS/SPARKLE-AUDIT-2026-07-03.md`.)
- **Stopped the bleeding (live).** Removed the bad v1.6 `<item>` from the live appcast so v1.4 users
  stop getting the "improperly signed" error. Newest entry is now v1.4 (140). Commit `d31b68a` on main.
- **Hardened future custody.** Commit `368f3d4` on main:
  - `scripts/release.sh`: pre-build guard (aborts unless Keychain key == embedded `SUPublicEDKey`),
    post-sign `sign_update --verify`, and `SPARKLE_ACCOUNT` for per-app Keychain accounts.
    Verified end-to-end (`bash -n` clean; run dies with the correct no-key message before archiving).
  - `05_Docs/sparkle-signing.md`: rewritten — key marked LOST, wrong "shared with SyncthingStatus"
    scope fixed, per-app + Strongbox custody model + rotation runbook.
  - **New** `~/ProgrammingProjects/1-macOS/SPARKLE-KEY-REGISTRY.md` — canonical cross-app key registry
    (app → key → account → backup status + do-now checklist). Not fabricating unique apps' public keys.
- **Git hygiene:** all main changes done in isolated worktrees off `origin/main`; `feature/in-app-help`
  WIP (56 files) untouched. Local `main` fast-forwarded — the prior "14 behind origin/main" drift cleared.

## Decisions
- Rotate CropBatch (and the other Group-A apps) to **new per-app keys**; adopt per-app Keychain
  accounts + mandatory Strongbox backup + a release-time key-match guard. (Full entry in decisions.md.)

## Next
- **User:** back up the keys that STILL exist to Strongbox before another is clobbered — priority
  Group B (`eH6jo…`) from the M1 Max (DiskVerdict is live), then Penumbra/Aloft (files on M4 Pro),
  then Magpie/TimeCodeEditor. Checklist in `SPARKLE-KEY-REGISTRY.md`.
- Then rotate CropBatch's key + ship v1.7 (runbook in `sparkle-signing.md`), with a one-time
  "download manually" notice for v1.4/v1.6 users.
- **Trap:** `feature/in-app-help` working-tree `appcast.xml` still has the bad v1.6 entry
  (uncommitted). Scrub it before that branch ever merges to main, or it re-introduces the bad feed.
- The session docs written today are uncommitted on `feature/in-app-help` (plain `/log`, no commit).
