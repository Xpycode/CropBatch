# Session: 2026-04-01a

## Goal
Code review of Grid Split feature + ContentView refactor before committing. Create implementation plan for fixes.

## Progress
- Triple-checked SPEC-grid-split.md against actual codebase — found 8 issues, patched all into spec
- Full code review of 15 changed files + 24 new files (-1,557 / +435 lines)
- Security review: clean — no secrets, `print()` replaced with `os.Logger`, CLI print is intentional
- Found 3 actionable issues, 2 nice-to-haves
- Created implementation plan: `05_Docs/PLAN-code-review-fixes.md`
- Updated TASKS.md with sprint items

## Code Review Findings

### Must Fix (before commit)
1. **DRY violation** — Grid tile URL construction duplicated in 3 places; `outputURL(for:index:gridRow:gridCol:)` exists but unused by save paths
2. **Broken index tracking** — `processAndExportWithRename` hardcodes `(0, url)` for non-conflicting results, breaking sort order
3. **Dead code** — `SidebarComponents/_Unused/` has 4 files from ContentView extraction

### Nice-to-Have
4. Corner radius PNG coercion also duplicated (solved by Fix 1)
5. `processAndExportInPlace` doesn't guard grid at runtime (UI blocks it)

### What's Good
- Pipeline deduplication done — `processImageThroughPipeline` is single source of truth
- Thread-safe watermark rewrite (lockFocus → NSBitmapImageRep)
- Solid test coverage (splitIntoGrid: 5 cases, ExportSettings: grid URL + collision)
- ContentView 1,785 → 258 lines

## Decisions
- **`applyGridSuffix` helper needed** — rename-conflict path can't re-run full `outputURL` (would double-suffix), needs lighter method
- **Drop index tuple in rename path** — `[URL]` return is sufficient, both paths already produce correct order

## Next
- Execute PLAN-code-review-fixes.md (3 fixes, all independent)
- Build + test verification
- Commit all changes
- Then: manual grid split testing, v1.4 release prep
