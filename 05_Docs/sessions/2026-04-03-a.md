# Session: 2026-04-03-a

## Goal
Production review before v1.4 release. Run /review checklist, scan code for issues, produce a fix plan.

## Progress
- Ran /review production checklist (interactive)
- Code reviewer agent scanned 14 modified + 6 new files
- Identified 1 High, 4 Medium findings
- Dismissed ThumbnailCache TOCTOU as false positive (actor prevents the race)
- Wrote PLAN.md with Wave 1 (mechanical fixes) + Wave 2 (UI feedback)

## Findings
| Severity | Issue | Status |
|----------|-------|--------|
| HIGH | `@MainActor` missing from 5 `@Observable` model classes | → planned |
| MEDIUM | `FolderWatcher.processNewFile` uses `DispatchQueue.main.asyncAfter` in @MainActor class | → planned |
| MEDIUM | Watermark load failure shows no user feedback | → planned |
| MEDIUM | ThumbnailCache.inFlight TOCTOU | FALSE POSITIVE — actor prevents it |
| LOW | Drop handler passes `isSecurityScoped: false` inconsistently | → planned |
| LOW | ExportSettingsView 1,484 lines | deferred — "works for me" policy |

## Decisions
- ThumbnailCache is correct as written — no TOCTOU possible without suspension points between check and write
- Watermark error: inline label preferred over alert (lower friction)
- File size refactor deferred until after v1.4

## Next
- Execute PLAN.md Wave 1: add `@MainActor` to 5 classes, fix FolderWatcher, fix drop handler
- Execute PLAN.md Wave 2: watermark error UI feedback
- After fixes: manual test grid split end-to-end
- v1.4 release prep (version bump, appcast, notarize)
